# Root dependency bundle for Vercel / local installs
# Pull in the project’s base Python stack
-r config/requirements.txt

# Additional libs used across analysis scripts and database utilities
PyYAML>=6.0.1,<7.0.0
pymongo>=4.6.0,<5.0.0
colorama>=0.4.6,<1.0.0
